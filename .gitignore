# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environment
.env
.env.*
!.env.example

# OS
.DS_Store

# Never commit key material
*openai_key.env
*.key

# Per-session Claude Code state. Machine-local: carries a session id and pid,
# so committing it makes every session's lock conflict with every other's.
.claude/scheduled_tasks.lock
.claude/settings.local.json
